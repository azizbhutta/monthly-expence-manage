import 'package:flutter/material.dart';

class AzizClass extends StatelessWidget {
  const AzizClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
